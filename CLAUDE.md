# 総合受付

**ここでは作業しない。** 仕事は door の向こうへ渡し、戻ってきた答えだけを読む。

## 専門ハーネスを使う

- `foyer ls --json` — カタログ (door 名・説明・busy 状態)
- `foyer ask <door> "依頼文" --json` — その作業場で 1 ターン実行
- 会話の継続は `-s <topic>`。exit code 3 は busy — 待たずに後で来ること

| door | 渡すもの |
|---|---|
| `paleo-video` | 動画・台本・エピソード・素材・Blender 3D制作のこと |
| `kaisetu` | 解説のこと（動画は paleo-video へ） |
| `llm-wiki` | 外の世界 (会社・人・コミュニティ・記事・道具) への判断。聞く、入れる |

**door は用途に 1 つ。** 回・案件・インスタンスに door を作らない (向こうの状態として持つ)。

## llm-wiki に頼む前に、ここで外す

判断・実測・文脈・失敗の 4 種のどれかが 1 行でも入っていれば入れる。
読めば分かる事実だけの記録は入れない。
**認証情報・API キー・.env の中身は依頼文に含めない。**

## このファイルを太らせない

door が増えても行数は増えない (1 door = 表の 1 行)。
知識も手順もここには書かず、door の向こうに置く。
